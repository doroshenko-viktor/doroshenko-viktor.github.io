{"pageProps":{"noteKey":["linux","linux-date-time"],"note":{"title":"Synchronization Of Date In Debian With NTP server","date":"2023-03-06","content":"\n<p>To setup <code>Debian Linux</code> to synchronize its date and time with an <code>NTP (Network Time Protocol)</code> server, follow these steps:</p>\n<pre><code class=\"hljs language-bash\">sudo apt install ntp\n</code></pre>\n<p>This will install the <code>ntp</code> package, which includes the <code>ntp daemon (ntpd)</code>.</p>\n<p>Configure the <code>ntp</code> daemon. Open <code>/etc/ntp.conf</code> file with root privileges:</p>\n<pre><code class=\"hljs language-bash\">sudo nano /etc/ntp.conf\n</code></pre>\n<p>\n  In the file, find the lines that specify the <code>ntp</code> servers to use. You can uncomment these lines and replace the default servers\n  with the ones you want to use. For example:\n</p>\n<pre><code class=\"hljs language-bash\">server time.google.com\n</code></pre>\n<p>Save and close the file.</p>\n<p>Restart the <code>ntp</code> daemon:</p>\n<pre><code class=\"hljs language-bash\">systemctl restart ntp\n</code></pre>\n<p>To verify that your Debian Linux system is synchronizing its date and time with the <code>ntp</code> server, you can run the following command:</p>\n<pre><code class=\"hljs language-bash\">timedatectl status\n</code></pre>\n"}},"__N_SSG":true}