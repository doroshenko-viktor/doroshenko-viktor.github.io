{"pageProps":{"noteKey":["rust","basics"],"note":{"title":"Rust Basics","date":"2022-03-03","content":"\n<h2>Never Type</h2>\n<p>\n  <code>!</code> is an empty type in Rust, which can never be instanciated. It usually used to indicate that\n  function or expression never returns value:\n</p>\n<pre><code class=\"hljs language-rust\"><span class=\"hljs-keyword\">fn</span> <span class=\"hljs-title function_\">func</span>() <span class=\"hljs-punctuation\">-></span> ! {\n}\n</code></pre>\n<p>\n  Also it used in <code>match</code> expressions, when <code>continue</code>, <code>panic</code> or similar operation executed in\n  some arm. <code>!</code> useful here, because all arms should return value of same type and <code>never type</code> helps\n  in this scenario.\n</p>\n<pre><code class=\"hljs language-rust\"><span class=\"hljs-keyword\">let</span> <span class=\"hljs-keyword\"></span><span class=\"hljs-variable\">guess</span>: <span class=\"hljs-type\">u32</span> = <span class=\"hljs-keyword\">match</span> guess.<span class=\"hljs-title function_ invoke__\">trim</span>().<span class=\"hljs-title function_ invoke__\">parse</span>() {\n    <span class=\"hljs-title function_ invoke__\">Ok</span>(num) => num,\n    <span class=\"hljs-title function_ invoke__\">Err</span>(_) => <span class=\"hljs-keyword\">continue</span>,\n};\n</code></pre>\n"}},"__N_SSG":true}